<div id=top>
  <a href = "../welcome"><%= image_tag "header_logo.png" %></a>
  <div id=log>
  <ul id=login>
    <li><a href="apps/new">Submit New App</a></li>
    <li><a href="#">Login</a></li>
    <li><a href="#">Sign Up</a></li>
</ul>
</div>

<div id=search>
  <p>Search: </p>
</div>
</div>

<div id=nav>
  <%= render 'side' %>
</div>

<div id = content>
<div id=form>
  <div id=top2><p>All Apps</div>
  <div id = inner>
 
<table>
	<tr>
    <th>Name</th>
    <th>Description</th>
    <th colspan="2"></th>
  </tr>
 
  <% @apps.each do |apps| %>
    <tr>
      <td><%= apps.name %></td>
      <td><%= apps.description %></td>
      <td><%= link_to 'show', app_path(apps) %></td>
      <td><%= link_to 'Edit', edit_app_path(apps) %></td>
      <td><%= link_to 'Destroy', app_path(apps),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

  </div>
  </div>
</div>